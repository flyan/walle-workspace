# 2026-03-26 日志

## 今天做的事

### 1. 日志习惯确认
- Leon 问我昨天干了什么，发现3月25日没有日志
- 确认原因：关机前没写，或者没开 session
- Leon 要求：以后每天都要写日志，哪怕只干了一件事也写一行
- Leon 还提了更重要的问题：直接关机可能导致我来不及写日志

### 2. 关机协议确立
- Leon 以后用完电脑会让我关机，而不是直接关
- 我负责在关机前写当天日志
- 通知其他代理（writer/coder）也建立同样习惯
- 如果当天确实没什么要记的，可以不写

## 待处理
- 下次开 writer/coder session 时跟他们确认关机写日志的习惯

---

## 下午补充（11:00-11:18）

### 3. OpenClaw web_search 工具现状确认
- OpenClaw 内置 `web_search` 支持的 provider：Brave / Perplexity / Gemini / Grok / Kimo，全部需要 API key
- **不支持 DuckDuckGo** 作为内置 provider
- Brave 需绑信用卡，中文效果差，不推荐

### 4. DuckDuckGo 搜索脚本安装与测试
- 安装了 `duckduckgo-search` 和 `ddgs` 两个 Python 包
- 创建脚本：`web_search_ddg.py`，使用 `ddgs` 包封装搜索
- **测试结论：DuckDuckGo 在当前网络环境下完全不可用**
  - `html.duckduckgo.com` 连接超时（网络层拦截，不是代码问题）
  - 百度、MiniMax API 均正常，说明是 DuckDuckGo 被特定拦截
  - 三个 agent 测试均失败，确认是网络问题而非脚本问题

### 5. 共享脚本目录迁移
- 将所有搜索/图像脚本从各自 workspace/skills 目录移到统一位置：
  - `C:\Users\flyan\.openclaw\scripts\web_search.py`（MiniMax MCP 搜索）
  - `C:\Users\flyan\.openclaw\scripts\understand_image.py`（MiniMax 图片识别）
  - `C:\Users\flyan\.openclaw\scripts\web_search_ddg.py`（DuckDuckGo，当前不可用）
- 所有 agent 可通过绝对路径访问，不依赖各自的 workspace/skills 目录
- writer agent 验证通过 ✅

### 6. 三 agent 双搜索测试（新天绿能新闻）
| Agent | MiniMax MCP | DuckDuckGo |
|-------|------------|------------|
| Writer | ✅ 成功 | ❌ 失败 |
| Coder | ✅ 成功 | ❌ 失败 |
| Secretary | ✅ 成功 | ❌ 失败（超时）|

- MiniMax MCP 搜索：所有 agent 全通，返回新天绿能 2025 年报（营收198.31亿，净利18.26亿+9.21%，拟10派2元）
- DuckDuckGo：全部网络层拦截失败

### 7. 模型切换测试
- Leon 切换到 Sonnet（anthropic/claude-sonnet-4-6）模型
- MiniMax MCP 搜索脚本（通过 exec 工具调用）不受模型影响，正常工作 ✅
- 结论：搜索能力与对话模型无关，依赖的是 exec 工具和 Python 脚本

### 当前可用搜索方案总结
1. **主力：MiniMax MCP** — `~/.openclaw/scripts/web_search.py`，中文强，所有 agent 可用
2. **备用：web_fetch 抓百度/东方财富** — 无需脚本，直接调用
3. **DuckDuckGo：不可用** — 网络被拦截，保留脚本等待网络条件改善
