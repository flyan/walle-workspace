# 2026-03-14

## 待办事项记录

### 用户 Leon 提到的待办事项：
1. **配置 Gemini 模型** - 需要设置 Gemini API 密钥并配置为可用模型
2. **配置多个 agent** - 需要设置多个 agent 实例或配置不同的 agent 角色

### 当前状态检查：
- 当前主要模型：openrouter/hunter-alpha:free
- 当前备用模型：openrouter/free, nvidia/nemotron-3-super-120b-a12b:free, healer-alpha:free, nvidia/nemotron-3-nano-30b-a3b:free, qwen/qwen3-next-80b-a3b-instruct:free
- 已配置模型别名：sonnet (claude-sonnet-4-6), opus (claude-opus-4-6), haiku (claude-haiku-4-5-20251001), deepseek (deepseek-chat), deepseek-r1 (deepseek-reasoner)
- **没有 Gemini 模型配置**
- 当前 agent 配置：支持最多 4 个并发 agent，8 个并发 subagent

### 配置 Gemini 模型需要：
1. Gemini API 密钥（从 Google AI Studio 获取）
2. 在 openclaw.json 的 agents.defaults.models 中添加 Gemini 配置
3. 可能需要添加对应的 auth 配置

### 配置多个 agent 选项：
1. 使用 subagents 功能运行多个实例
2. 配置不同的 agent 角色和模型
3. 设置不同的工作空间或任务

### 后续步骤：
1. 获取 Gemini API 密钥
2. 更新 openclaw.json 配置
3. 测试 Gemini 模型
4. 设计多个 agent 的工作流程
5. 设置提醒机制（通过 HEARTBEAT.md 或 cron）