# 金融科技与 AI 深度情报采集方案 (Intelligence Hub Protocol)

## 📋 方案内容 (2026-03-14 23:51)

1. **情报源池**：基于 `FINTECH_ACCOUNTS.md` 中的 50+ 个顶级公众号。
2. **频率控制**：手动触发模式（也可配置为早晨 8 点定时触发）。
3. **筛选标准**：
   - **时间**：强制最近 14 天（或 7 天）内的文章。
   - **深度**：质量评分 > 60（使用 `analyze_quality.py`）。
4. **输出结构**：倒金字塔结构。
   - 核心摘要在前（本周 3 大趋势）。
   - 详细内容在后。
5. **归档管理**：存放在 `workspace/reports/` 并同步 GitHub。

## 🛠️ 优化要求 (由用户 767224 指定)

### 1. 链接策略
- **链接位置**：所有链接必须统一汇总到报告的最末尾。
- **优先原链**：强制通过 `-r` (resolve-url) 解析搜狗重定向。
- **优先级**：`mp.weixin.qq.com` 原链 > 搜狗链接。只有解析失败时才使用搜狗链接。

### 2. 样本量扩展
- **数量提升**：目前抓取的文章数量过少（之前的 5 篇太少）。
- **时长允许**：采集过程可以运行更长时间，对名录中的所有重点号进行扫描。
- **覆盖率**：针对每个公众号的抓取上限应提高（如 `-n 20` 或更高）。

### 3. 来源多样化 (Diversity Policy)
- **去中心化**：避免报告内容过度集中于少数几个公众号。
- **多维度视角**：如果一个公众号产出的高分内容较少，必须自动切换到名录中其他同领域的公众号进行扫描。
- **独立性**：确保情报来自多个独立的立场和视角，而不是重复同一家的声音。

## 📝 开发进度

- [x] 完成顶流公众号名录整理 (`FINTECH_ACCOUNTS.md`)
- [x] 完成第一份样例报告测试
- [ ] 优化 `generate_intelligence_report.ps1` 脚本以符合上述优化要求
- [ ] 实现来源多样性自动轮换逻辑
- [ ] 测试全文提取与深度总结流程

---
*由主 Agent 维护，用于指导子 Agent 执行任务*
